import React from 'react';
import './_Con1080.scss'
const Con1080 = (props) => {
    return (
        <div className="Con1080">
            {props.children}
        </div>
    );
};

export default Con1080;